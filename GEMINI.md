# Gemini Workspace Context

Canonical workspace contract is `AGENTS.md`.

Before implementation, read `AGENTS.md` and `docs/00_DECISIONS_AND_PRECEDENCE.md`. Do not invent alternative product rules. Use the workspace skills under `.agents/skills/` and delegate independent reviews to `.agents/agents/` where appropriate.
